using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//may or may not be needed

public static class StaticValues
{
    //returns true while the player is allowed to act
    public static bool playerCanAct = false;
    //selection false = no selection, true = player selected
    public static bool selection = false;
}
